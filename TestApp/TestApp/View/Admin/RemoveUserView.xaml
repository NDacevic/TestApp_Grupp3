<Page
    x:Class="TestApp.View.Admin.RemoveUserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestApp.View.Admin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:TestApp.Model"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <StackPanel>
            <TextBlock       Text= "Var vänlig välj Anställd eller Elev för att få fram information. Sök även på ID för en direkt träff."
                             Margin="40,40,0,0"/>
            <StackPanel      Orientation="Horizontal"
                             Margin="40,0,0,0">
                <RadioButton x:Name="EmployeeRadioBtn"
                             Content="Anställd" Click="EmployeeRadioBtn_Click"/>
                <RadioButton x:Name="StudentRadioBtn"
                             Content="Elev" Click="StudentRadioBtn_Click"/>
            </StackPanel>
            <TextBox         PlaceholderText="Skriv in ID.."
                             Margin="40,0,0,0"
                             x:Name="SearchIdTxtBox" KeyDown="SearchIdTxtBox_KeyDown"
                             />
        </StackPanel>
        <ListView BorderBrush="Black"
                  BorderThickness="2"
                  Margin="40,150,40,200"
                  ItemsSource="{x:Bind adminViewModel.AllUsers}"
                  x:Name="DisplayUsersLV"
                  >
            <ListView.ItemTemplate>
                <DataTemplate  x:Name="displayUsersLV" x:DataType="data:Student" x:DefaultBindMode="TwoWay" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind FirstName}"/>
                        <TextBlock Text="{x:Bind LastName}"/>
                        <TextBlock Text="{x:Bind Email}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

      
    </Grid>
</Page>
